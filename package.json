{
  "name": "gildedrose",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "lint": "standard ./src/**",
    "lint:fix": "standard ./src/** --fix",
    "test": "jest --coverage --no-watchman",
    "test:watch": "jest --no-watchman --watchAll"
  },
  "dependencies": {
    "jest": "^23.0.1",
    "lodash": "^4.17.10"
  },
  "devDependencies": {
    "standard": "^11.0.1"
  },
  "eslintConfig": {
    "env": {
      "es6": true,
      "jest": true
    }
  }
}
